~~~~~~~~~~~~~~~~~
TeamCity Plugins
~~~~~~~~~~~~~~~~~

To build the project:
---------------------

* Run "gradle" or "gradle clean build"

To deploy all plugins:
----------------------

* Make sure TC server is not running
* Run "gradle clean build deploy -DteamCityDir=<TC directory>"
